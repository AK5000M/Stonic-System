import React from "react";
import { LoginContent } from "@/page-contents/auth/login";

const Login = () => {
	return (
		<React.Fragment>
			<LoginContent />
		</React.Fragment>
	);
};

export default Login;
